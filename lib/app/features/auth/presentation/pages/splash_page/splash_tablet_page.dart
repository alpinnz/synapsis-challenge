import 'package:flutter/material.dart';

class SplashTabletPage extends StatelessWidget {
  const SplashTabletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Center(child: Text("SplashTabletPage"))),
    );
  }
}
